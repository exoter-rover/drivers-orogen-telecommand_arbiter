# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(telecommand_arbiterTaskLib)
ADD_LIBRARY(${TELECOMMAND_ARBITER_TASKLIB_NAME} SHARED 
    ${TELECOMMAND_ARBITER_TASKLIB_SOURCES})
add_dependencies(${TELECOMMAND_ARBITER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${TELECOMMAND_ARBITER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TELECOMMAND_ARBITER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TELECOMMAND_ARBITER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TELECOMMAND_ARBITER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${TELECOMMAND_ARBITER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${TELECOMMAND_ARBITER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TELECOMMAND_ARBITER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TELECOMMAND_ARBITER_TASKLIB_HEADERS}
    DESTINATION include/orocos/telecommand_arbiter)

